#a)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('the probability of getting between 54 and 60 heads is %.4f',prob)
#b)
# a function simulate 100 flips
flip<- function() {
#flip a coin 100 times
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('the probability of getting between 54 and 60 heads is %.4f',prob)
flip<- function() {
#flip a coin 100 times
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
flip<- function() {
#flip a coin 100 times
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(123)
#run the simulation 10000 times
x<- replicate(10000,flip())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flip<- function() {
#flip a coin 100 times
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(123)
#run the simulation 10000 times
x<- replicate(10000,flip())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flip<- function() {
#flip a coin 100 times
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(123)
#run the simulation 10000 times
x<- replicate(10000,flip())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('the probability of getting between 54 and 60 heads is %.4f',prob)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('the probability of getting between 54 and 60 heads is %.4f',prob)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('the probability of getting between 54 and 60 heads is %.4f',prob)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('By calculation, the probability of getting 54~60 heads is %.4f',prob)
prob1<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('By calculation, the probability to get 54~60 heads is %.4f',prob1)
flip<- function() {
#flip a coin 100 times
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(123)
#run the simulation 10000 times
x<- replicate(10000,flip())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(123)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(987)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(10000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(1000000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(100000000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(10000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(1000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(123)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(10)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(100)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(1000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(10)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(100)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(1000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(10000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(100000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(1000000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(10000000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(2)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(2)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(2)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(2)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(200000000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(10000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#set the random seed
set.seed(10000)
#run the simulation 10000 times
x<- replicate(10000,flips())
# compute the fraction of results between 54 and 60
f<-sum(x>=54 & x<=60)/10000
sprintf('the fraction of results between 54 and 60 is %.4f',f)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(10000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(10000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('By calculation, the probability to get 54~60 heads is %.4f',prob)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(10000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('By calculation, the probability to get 54~60 heads is %.4f',prob)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('By calculation, the probability to get 54~60 heads is %.4f',prob)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(10000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('By calculation, the probability to get 54~60 heads is %.4f',prob)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(10000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(10000000,flips())
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(100000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(100000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(100000)
x<- replicate(100000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(100000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
prob<-pnorm(60.5,50,5)-pnorm(53.5,50,5)
sprintf('By calculation, the probability to get 54~60 heads is %.4f',prob)
flips<- function() {
s<- sample(c(0,1),size=100,replace=TRUE)
return(sum(s))
}
#the random seed
set.seed(10000)
x<- replicate(10000,flips())
prob2<-sum(x>=54 & x<=60)/10000
sprintf('By simulation, the fraction to get 54~60 heads is %.4f',prob2)
